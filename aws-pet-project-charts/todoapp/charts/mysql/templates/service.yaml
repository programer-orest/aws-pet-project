apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.mysql.service.serviceName}}
  namespace: {{ .Values.common.namespace }}
spec:
  selector:
    app: {{ .Values.common.mysqlSelector }}
  ports:
  - name: {{ .Values.mysql.portName }}
    port: 3306
  clusterIP: None