apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: todoapp-pvc
  namespace: todoapp
spec:
  volumeName: pv-storage
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
